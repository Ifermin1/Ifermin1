# Código original (Flet, escritorio)

Copia sin modificar del contenido de `TradePilot X.zip` (sin el `.venv`). Se conserva solo como referencia durante la migración; la versión activa está en `engine/` y `web/`. Se puede borrar cuando el motor nuevo esté validado con NinjaTrader real.
